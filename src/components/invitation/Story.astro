---
import Scene from "./Scene.astro";
import { invitation } from "../../content/invitation";
import { invitationAssets } from "../../content/invitation-assets";
---

<Scene id="beginning" image={invitationAssets.story.couple} natural>
  <div class="max-w-2xl rounded-sm bg-black/35 p-7 backdrop-blur-[2px] sm:p-10 lg:ml-auto">
    <h2 class="font-script text-6xl leading-none text-invitation-gold sm:text-7xl">
      {invitation.story.title}
    </h2>
    <div class="mt-8 space-y-6 font-serif text-lg leading-relaxed italic sm:text-xl">
      {invitation.story.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
    </div>
  </div>
</Scene>
